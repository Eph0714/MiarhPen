<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Lets MiarhPen ask, in-app, to be exempted from Android's battery
         optimization / background-freezing — no manual Settings digging
         required from the user. See MainActivity.kt +
         lib/core/platform/battery_optimization_service.dart. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- MiarhPen is offline-first for everything else, but the optional
         "Sync to MySQL (Desktop)" feature in Settings > Data needs a raw
         TCP socket to reach a MySQL server on the local network. Without
         this permission, release builds fail every such connection with
         "Operation not permitted" (debug builds get network access
         implicitly, masking the problem until release). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required by local_auth for fingerprint/face unlock. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Recurring Payments' reminder alarm: post the notification itself
         (required at runtime on Android 13+), schedule it to fire at an
         exact day/time (Android 12+ gates exact alarms behind this
         permission), and re-register it after the device reboots so a
         schedule set before a restart still fires afterward. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:label="MiarhPen"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <!-- MiarhPen holds a local financial database and must not be
             silently backed up to / restored from the user's Google
             account by Android's automatic Auto Backup feature. Without
             this, uninstalling the app does NOT actually clear its local
             data on a device with cloud backup enabled — Android restores
             the previous install's database (including any corrupted
             state) right after reinstall, which is misleading during
             testing and is not appropriate for sensitive financial data
             the app already has its own explicit Backup/Restore feature
             for (see Settings > Data). -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: re-registers Recurring Payments'
             scheduled alarms after the device reboots (without this, every
             exact alarm set via zonedSchedule is silently wiped by Android
             on restart) and actually delivers each one when its time
             arrives. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
